name: shared
description: "Shared models, services, and constants for IR HVAC apps."
version: 0.0.1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.1.3
  cloud_functions: ^6.0.7
  firebase_storage: ^13.1.0
  image_picker: ^1.2.1
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # Assets provided by consuming apps; shared services reference paths only.
